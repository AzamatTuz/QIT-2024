const car = {
    brand: 'Toyota',
    model: 'Camry',
    year: 2020,
    color: 'black',
    isOK: true,
    country: 'Japan'
}

car.isOK = false;
car.owners = 5;
console.log(car);


console.log(`${car.brand} ${car.model} koligi ${car.year} jyly jaryg kordi`);